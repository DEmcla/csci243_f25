# PostgreSQL Configuration
# IMPORTANT: Copy this file to .env and update all passwords!
POSTGRES_USER=csci243_user
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD
POSTGRES_DB=csci243_db

# pgAdmin Configuration  
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=CHANGE_THIS_PASSWORD

# Security Notes:
# 1. Never commit the actual .env file to git
# 2. Use strong passwords (mix of letters, numbers, symbols)
# 3. Different passwords for each service
# 4. For production, use secrets management tools